{
  "rules": {
    "presence": {
      ".read": true,
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        "$tab": {
          ".validate": "$tab.length <= 20 && newData.isBoolean()"
        },
        ".validate": "newData.hasChildren()"
      }
    }
  }
}
